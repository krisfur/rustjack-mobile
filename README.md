# Rustjack Mobile
Mobile version of Rustjack, using eGUI.

## compile for desktop
```bash
cargo run
```

## Compile for Android
This uses xbuild and adb (you need the android SDK in your path like `export PATH=$PATH:~/Android/Sdk/platform-tools`):
```bash
bash build_android.sh
```